name = "starhub"
main = "functions/api/getToken.ts"
compatibility_date = "2026-05-27"

# 静态文件托管（Cloudflare 新架构：Pages 融合到 Workers）
[assets]
directory = "dist"

# 环境变量（CLIENT_ID 可公开，CLIENT_SECRET 用 secret 加密存储）
[vars]
CLIENT_ID = "Ov23liKH9x1DCueWwcud"
